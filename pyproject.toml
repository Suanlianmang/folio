[project]
name = "folio"
version = "0.1.0"
description = "Terminal tool for design exploration tracking"
readme = "README.md"
requires-python = ">=3.14"
dependencies = []

[project.scripts]
folio = "cli:main"

[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"
